<? if ($this->messages): ?>
    <div class="alert in alert-block fade alert-success"><a class="close" data-dismiss="alert">×</a>
        <? foreach ($this->messages as $message): ?>
            <p><?= $message ?></p>
        <? endforeach; ?>    
    </div>
<? endif; ?>

<?
$form = $this->form;
$form->setAttribute(
        'action', $this->url('login/process', array('action' => 'authenticate'))
);
$form->prepare();
?>
<?= $this->form()->openTag($form) ?>
<div id=""><legend>Sign in</legend></div>
<div class="body">
    <?= $this->formCollection($form) ?>
</div>
<?= $this->form()->closeTag() ?>

<div id="myModal" class="modal hide fade login-wraper" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <div class="modal-body">
        <?
        $form = $this->form;
        $form->setAttribute(
                'action', $this->url('login/process', array('action' => 'authenticate'))
        );
        $form->prepare();
        ?>
        <?= $this->form()->openTag($form) ?>
        <div id="myModalLabel"><legend>Sign in</legend></div>
        <div class="body">
            <?= $this->formCollection($form) ?>
        </div>
        <?= $this->form()->closeTag() ?>   
        <br><br><button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
    </div>
</div>