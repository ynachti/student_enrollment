<?php
use Zend\Form;

$this->headTitle()->prepend('Contact');
?>
<section class="contact">
	<h2>Contact me</h2>
	<?php if($this->error): ?>
	<p class="error">There was an error in your submission</p>
	<?php  endif; ?>

	<?php
	$form = $this->form;
	$form = setAction($this->url('contact/process'));
	$form = setMethod('post');
	echo $form->render($this);
	?>
</section>